# V1.1 Discord 完整时间窗采集与观点阅读 Engineering Journal

日期：2026-07-22
状态：**时间窗实现、双来源初始覆盖边界、作者 selector 修订及其专用数据库 migration 已完成；作者 selector 控制面生产部署、真实 Discord 范围验收、普通用户审阅与 launchd 安装仍待执行。**

## 目标与边界

V1.1 将 V1 的“按页数抓取”改为“可证明完整的时间窗抓取”：每个来源按上海时间 00:00、08:00、16:00、20:50 建立不可变的 `(start_at, end_at]` 范围。任务只有在持久化范围内事实、摘要和证据，并已验证到达下界后，才能推进来源的 `coverage_through_at`。

本阶段同样增加频道级稳定作者配置、日内作者观点卡、署名话题观点，以及管理员“更新至当前时间”。普通读者只得到安全的内容投影，不得到原始正文、来源 ID、游标、Worker/Provider/Prompt 或本地引用。

本次没有扩大到 X、媒体内容解析、外部文章正文、第二套浏览器采集框架、普通用户手动任务或新的 Provider。

## 已完成的实现

| Task | 结果 | 提交 |
| --- | --- | --- |
| 1 | 时间范围、coverage waterline、resume progress、分段与完整范围回执的数据库/契约 | `58df9ae` |
| 2 | 管理员初始化覆盖边界、已观察稳定作者配置、服务端固定时间的手动更新 API | `2541229` |
| 3 | Worker 领取不可变范围、逐页持久化/续租、最终 `range-complete` 回执 | `e5079dc` |
| 4 | Active Adapter/runtime 按时间下界迭代，移除 V1.1 的页数型成功条件 | `6903897` |
| 5 | 上海四窗口、无数量截断的补窗、每分钟安全 tick、受限 launchd 模板与 check-only 脚本 | `facba3d` |
| 6 | chunk 事实层与日累计作者/话题层的严格 Schema；每日输出只引用已验证事实单元 | `3346889`、`605ae87` |
| 7 | 安全作者配置界面、管理员手动更新入口、内容优先的 `/discord` 作者/话题阅读页 | `55ad3fe` |
| 8（本地部分） | 公开 fixture 跨层 E2E、真实实现聚焦回归、文档与独立审阅 | 本提交 |

## 关键实施决定

- **页数不是完成条件。** `max_pages` 被拒绝为新的 V1.1 任务语义；分页仅是浏览器读取的传输细节。长历史通过逐页 segment 和 `resume_cursor` 恢复，直到页面时间下界到达 `start_at` 或已验证空页证明历史耗尽。
- **覆盖水位与恢复游标分离。** `coverage_through_at` 只在完整范围回执的来源级事务中前移；`resume_cursor` 只服务于未完成任务，不能将失败区间伪装为已覆盖。
- **时间边界由可信控制面固定。** 定时/手动任务的 `end_at` 创建后不可变；实际运行时较晚的新消息不进入该任务。手动任务复用同来源尚未完成任务，避免并发重叠范围。
- **作者与观点必须可归因。** 管理员输入的显示名或用户名是配置事实来源；已观察作者只提供快捷建议，绝不构成保存门槛。任务逐页持久化 Canonical 后，服务端仅在唯一 stable ID 证据存在时解析为 resolved；零候选保持 pending，多候选标为 ambiguous。日累计卡片包含作者、市场/个股核心逻辑、操作倾向、方法论和不确定性；话题观点带作者与受控证据关系。未解析媒体只生成不确定性提示，不推断内容。
- **阅读 DTO fail closed。** 读者页不返回 raw 正文、本地 raw 引用、内部 ID、游标或 Provider 诊断；V1.1 结构不完整或媒体提示不一致时不会投影为可读总结。

## 本地验证证据

所有验证只使用本地数据库或公开人工 fixture；没有使用真实 Discord 内容、Cookie、来源 ID、私有 Prompt 或本地 evidence。

| 检查 | 命令或入口 | 结果 |
| --- | --- | --- |
| 数据库 | `supabase test db` | 11 个 pgTAP 文件、186 条断言通过 |
| Worker 全量 | `PYTHONPATH=workers/v0/src workers/v0/.venv/bin/python -m unittest discover -s workers/v0/tests -p 'test_*.py' -v` | 82 项通过 |
| 控制面全量 | `npm test && npm run lint && npm run build`（`apps/control-plane`） | 18 个测试文件、79 项通过；lint/build 通过 |
| V1.1 E2E runner | `bash scripts/v1/run-v1-1-e2e.sh` | 公开跨层 fixture 5 项 + 真实 Worker/runtime/scheduler 10 项 + 控制面 API/作者 selector/Reader/Presentation 46 项通过 |
| 发布前静态检查 | `bash scripts/v0/redact-check.sh`、`git diff --check` | 通过；未暂存 `docs/intake.md` 的用户改动 |

V1.1 runner 额外把如下内容放在同一条可重跑命令中：四个上海窗口与幂等、超过 100 页才到达下界且中断恢复无重复、漏窗排序和单来源失败隔离、`end_at` 排除、手动任务复用、作者五类内容/多作者话题/未解析媒体提示，以及普通用户无管理员操作和 375px 摘要 DOM 中没有“Evidence-backed messages”模块。

## 审阅与修正记录

Task 6 独立审阅发现日累计输出可能引用“同日但不在已验证事实单元”的消息 ID；`605ae87` 已将 daily parser 限制为事实单元证据集合。

Task 7 独立审阅发现未解析媒体覆盖标记与 warning 不一致时可能投影出总结；`55ad3fe` 已采用 fail-closed 投影。

Task 8 首次 E2E 审阅发现三项假绿灯：101 页 fixture 没有真正抵达下界、手动任务仅对相同时间点击去重、runner 只运行内存模型。修正后，fixture 只在抵达下界或验证空页后完成；更晚的第二次手动点击复用未完成任务；runner 也执行真实 Worker/runtime/scheduler、控制面 API、Reader 和 Presentation 测试。独立复审结论为通过。

### 会话可操作性热修复（2026-07-22）

真实验收进入管理员配置前发现：普通用户访问 `/admin` 会先得到服务端 403、随后被静默导向 `/discord`；同时产品没有登出 API 或页面入口，用户无法切换回管理员账号。根因不在 RLS 或角色判定，而在管理员 layout 将 403 重定向至首页、首页又按普通用户角色重定向至阅读页，以及 UI 未暴露会话终止操作。

用户确认热修复后，`082e5b7` 做了最小修正：保留原有服务端 `requireRole("admin")`；普通用户改到说明边界且可切换账号的 `/forbidden`；阅读页和管理员页右上角显示当前账号与角色，并提供由服务端 Supabase SSR client 执行的“退出 / 切换账号”。接口只返回受控成功/失败状态，不返回认证材料。新增回归覆盖管理员/阅读页账户控件、登出 API、403 目的地和无权限页。验证为控制面 17 个测试文件/76 项、lint、production build、V1.1 E2E、redaction 与 diff 检查通过。

专用 V1 远程数据库 migration 已完成且历史匹配。`082e5b7` 已部署至专用 V1 生产项目；管理员生产页确认保留 `/admin`，账号/角色/切换按钮可见，375px 下控件未溢出。为不打断当前管理员会话，没有在生产点击登出；登出成功路径由自动化接口测试覆盖。

随后真实 Sources 页核验发现覆盖初始化 API 没有表单入口，无法进行首次采集。`54c472d` 新增每来源的“首次采集范围”表单：日期默认留空，管理员必须选择日期与 00:00、08:00、16:00、20:50 之一的上海边界；初始化成功后仅显示连续覆盖状态，不能从页面更改。生产确认两个来源均出现该控件和四个边界选项。控制面全量验证更新为 18 个测试文件/77 项通过；V1.1 E2E、lint、build、redaction 与 diff 检查继续通过。

### 采集范围接口与首次边界生产修复（2026-07-22）

首次生产填写发现浏览器自动化无法可靠地驱动原生日期控件。`30f6f0b` 将输入改为显式的 `YYYY-MM-DD` 文本格式，并在客户端验证该日期确实对应上海自然日；控制面全量测试、lint、production build、V1.1 E2E、redaction 与 diff 检查均通过。

随后生产读取与提交均失败。根因是根目录 `.gitignore` 的宽泛 `coverage/` 规则把 API 路径名为 `coverage` 的目录忽略了：本地工作树能编译该路由，Git 和 Vercel 部署却没有这个文件。`7afd7b6` 将忽略规则限定到根目录的测试产物，并将 API 路由正式纳入版本控制；还增加了“读取失败向客户端保持通用错误、服务端仅记录安全诊断”的回归测试。最新验证为控制面 18 个测试文件/78 项、lint、production build、V1.1 E2E、redaction 与 diff 检查均通过。

修复部署 Ready 后，管理员在正式 Sources 页将**两个逻辑来源**都初始化为 **2026-07-21 20:50（Asia/Shanghai）**。两行都回显“连续覆盖从 2026 年 7 月 21 日 20:50 开始，当前已确认至同一时点”；这只建立增量采集水位，尚未抓取或总结任何新的 Discord 内容。

### 指定作者自由输入与延后身份解析修订（2026-07-22）

管理员指出“已观察到的作者”来自已持久化的 Canonical message（按来源读取最近的安全身份字段），因此不能成为配置约束；一个当前可在 Discord 中看到的重点作者可能尚未出现在此前持久化的消息样本中。问题本质是把“系统已有证据”误当成“管理员意图的许可条件”。

经用户确认后，新增 `011_v1_1_author_selector_resolution.sql`：配置保存 `requested_author`、`pending|resolved|ambiguous` 状态与可空 stable ID。新窗口任务冻结 selector 快照；Worker 在每页 raw/Canonical 持久化完成后、结构化理解前调用受当前租约约束的解析端点。该端点只回传任务快照中唯一匹配的 stable ID、显示名和可空 handle：无候选保持 pending；同名对应多个 stable ID 标为 ambiguous；没有任何分支会猜测绑定或返回聊天正文。历史的已解析配置保持 resolved，旧任务快照仍可执行。

本地迁移/pgTAP、控制面、Worker、契约、lint 和 production build 全部通过；专用 V1 Supabase 已回读确认 `011` 成功应用。控制面生产部署尚待完成，届时管理员可直接保存已确认的重点作者，并以真实时间窗验证解析生效。

## 未执行的授权门禁

本地验证与已部署控制面不等于 V1.1 已验收或正式可用。以下动作仍**尚未执行**，需用户再次明确授权后按已批准 Plan 的顺序推进：

1. 部署修订后的控制面；管理员直接输入每个来源的重点作者，再对 owner-only runtime 的真实 Discord 正常窗口与手动范围采集，含失败恢复和作者 pending/resolved/ambiguous 状态生效；
2. 普通用户真实 `/discord` 桌面与 375px 审阅；
3. Worker 领取/完成和真实 RLS 验收；
4. 确认 plist label 与 owner-only 配置路径后的 launchd 安装；
5. 普通用户真实生产审阅。

代码已与 GitHub `origin/main` 同步；本 journal 的本次状态更新将在同一收口中同步。在这些门禁完成前，V1.1 的准确状态是“时间窗代码、远程 migration、控制面和初始 coverage 已生效，等待真实内容验收和本地 Agent 安装”，而不是“正式可用”。
