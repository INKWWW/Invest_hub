# V0 工程日志——2026-07-18

## 2026-07-19 收口与远程部署补充

- 已修正 Vercel/Supabase 环境变量模板，并确认本地 `.vercel` 和环境文件不进入 Git。
- 新增远程 Worker 持久化协议与第 002 号数据库迁移：远程数据库保留原始数据本地引用、规范消息、结构化输出、证据关联和按任务尝试绑定的持久化收据。只有收据存在且消息计数、结构化运行 ID 与结果一致时，`accept_task_result` 才会推进 checkpoint；伪造 `persisted` 标记被拒绝。
- 新增显式授权的本地 Worker 运行时：只复用 Spike-01 验证过的 Browser Bridge 网络读取语义；真实模式需要 `V0_REAL_DISCORD_ACK=authorized`、owner-only 本地配置、私有 Prompt、OpenCLI contract、evidence 目录和一次性 Worker 邀请码。未授权或预检失败会拒绝运行，不把空数据写成成功。
- 新增管理员逻辑来源与手动同步任务入口；界面不接收频道 URL、Profile、Prompt、Cookie 或正文。
- 本地验证结果：控制面 30 个测试、Worker 46 个测试、确定性 V0 E2E 8 个测试、Supabase pgTAP 55 条断言、lint、生产构建和脱敏检查均通过。
- 已将第 002 号迁移应用到隔离 Supabase 项目，并确认远程迁移为 `001/002`；最新隔离 Vercel 预览构建状态为 Ready。Vercel 的受保护预览不接受无会话探针，但通过其受保护部署通道已抵达应用路由并完成合成 HTTP 验收：注册 → 心跳 → 领取 → 持久化 → 回报结果。
- 远程 HTTP 验收使用一条明确标记为合成、非 Discord 的逻辑来源和任务；第 2 次尝试成功，数据库回读确认原始 1 条、规范 1 条、结构化运行 1 条以及检查点 `v0-http-smoke-checkpoint-1` 一致落库。验收工作节点和临时邀请均已撤销或删除，日志未记录其明文凭据。
- 修复了一个仅在部署时暴露的契约加载缺陷：Vercel 的项目根目录是 `apps/control-plane`，原加载器试图读取部署目录之外的契约文件，导致合法心跳被统一映射为 `invalid_heartbeat`。现将只读契约包随控制面部署，并以测试逐字比对其与仓库根目录的规范契约，防止漂移。
- 远程角色与恢复补测已完成：临时普通用户登录后访问管理员来源接口被拒绝；合成任务的第一租约被显式置为到期后，第二工作节点以第 2 次尝试重新领取，并继承预置检查点 `v0-lease-checkpoint-before`。临时账号、工作节点、来源、任务、邀请码、会话和本地凭据均已删除。
- 已完成一次真实 Discord 有界单页验证。首次任务在无界历史翻页时于 90 秒采集截止后被控制面标记为 `retryable_failed`，失败分类为 `timeout`，安全检查点保持为空；该失败没有被当作空数据成功。随后增加“每个 V0 任务只消费一个通过新鲜度校验的页面”边界，并以公开测试验证仍保留新鲜度、缓存规避重开、错误游标、缺失/陈旧响应与超时的失败保护。
- 管理员将同一任务重试后，第 2 次尝试成功。受保护管理页确认：20 条原始记录、20 条规范记录、20 条重复计数、0 条未解决关系、0 条未解析媒体、1 条 `codex_cli` 结构化运行、20 个消息证据关联、20 个本地原始引用关联和非空安全检查点。真实正文、频道地址、浏览器配置档路径、Cookie、邀请码、提示词、完整模型响应和本地证据均未进入本日志或 Git。

## 范围与结论

本记录只覆盖 V0 基础设施与技术验证。确定性控制面、工作节点、Discord Active Adapter、Provider 边界、结构化证据、管理员调试页和恢复测试框架已完成；远程核心工作节点 HTTP、普通用户权限、租约恢复补测和一次真实 Discord 有界单页闭环均已通过。因此 V0 结论为**通过**，但这不是生产发布批准，也不表示已完成历史回填、长期稳定性或持续运营验证。

## 执行日志

| 阶段 | 实际尝试 | 结果 | 恢复/后续动作 | 证据引用 |
| --- | --- | --- | --- | --- |
| 契约 | Python/TypeScript 契约拒绝与测试样例检查 | 通过 | 保留跨语言数据结构作为唯一协议来源 | `tests://v0/contracts` |
| 数据库/RLS | 本地 Supabase 数据库迁移与 pgTAP | 通过，44 条断言 | 复跑 `supabase test db` | `db://v0/rls/001` |
| 控制面 | API 授权、任务状态、租约与幂等测试 | 通过，28 个控制面测试 | 增加脱敏任务详情路由 | `tests://v0/control-plane` |
| Worker | 注册、凭据权限、心跳、领取、结果与恢复测试 | 通过，46 个 Worker 测试 | 保留 `recovering` 恢复中状态和有限租约清理 | `tests://v0/worker` |
| Discord 边界 | Active Adapter 新鲜度、过期/缺失响应与页面截止时间测试 | 通过 | 未把失败响应当作空数据成功 | `tests://v0/active-adapter` |
| 提供方 | Mock 重试、Codex 只读/临时命令、JSON/数据结构与进程组超时测试 | 通过 | 重试只作用于当前分块；完整响应只留在本地证据 | `tests://v0/provider` |
| 管理员调试 | 状态视图模型、脱敏、重试门禁、角色边界与生产构建 | 通过 | 普通用户收到 403/重定向；视图不包含凭据字段 | `tests://v0/admin` |
| 确定性端到端 | 内存控制面 + 公开测试样例 + Mock 提供方 + 故障注入 | 通过，V0 E2E 8 个测试 | 原始数据/规范数据/提供方/租约失败时检查点保持不变 | `e2e://v0/deterministic` |
| 脱敏 | 仓库凭据形态值与禁入文件扫描 | 通过 | 未提交真实测试样例/证据 | `checks://v0/redaction` |
| 真实 Discord | 第 1 次无界翻页超时；修复为有界单页后第 2 次完成真实采集、Codex 结构化、持久化、回报与安全检查点 | 通过 | 只验证一个新鲜页面；不宣称历史回填或持续运营能力 | `deploy://v0/real-discord-bounded-batch-20260719` |
| 远程部署核心链路 | 隔离 Supabase `001/002`、Vercel 预览 Ready；合成 HTTPS 注册 → 心跳 → 领取 → 持久化 → 回报结果通过 | 通过 | 工作节点、临时邀请与本地临时证据均已清理；不含真实 Discord 内容 | `deploy://v0/http-smoke-20260719` |
| 远程角色/恢复 | 临时普通用户访问管理员接口被拒绝；第一租约到期后由第二工作节点以第 2 次尝试重新领取，并继承预置检查点 | 通过 | 全部对象为合成验证数据，已删除 | `deploy://v0/remote-role-recovery-20260719` |

## 重要修复

1. Supabase 邀请消费现在区分 `user` 和 `worker` 用途；Worker ID 不再被强制关联到认证用户外键。
2. Provider 超时清理会关闭 stdin，终止整个进程组，并且只在有界的清理窗口内等待。
3. 结构化输出会拒绝未知、非媒体或缺失的 `media_source_message_ids`；该集合必须与当前分块中未解析媒体的 ID 完全一致。
4. Next.js 构建最初无法识别 TypeScript 7 的包布局。现在控制面固定使用 TypeScript 5.9.3 及匹配的解析器，`npm ci` 可以复现该环境。
5. 端到端 shell 入口会拒绝低于 3.11 的 Python，而不是输出容易误导的 Worker 导入回溯信息。
6. 控制面部署现在携带只读 V0 契约包；测试会逐字验证部署包与仓库规范契约一致，避免本地可读而 Vercel 运行时缺文件的差异。
7. Vercel 命令行创建会话 Cookie 文件时必须预先设为 `0600`；本次补测发现默认权限过宽后已立即收紧并删除，后续远程验收不得复用默认权限文件。
8. 真实页面首次运行暴露出无界历史翻页会耗尽 V0 的采集时间预算。V0 验收批次现明确限制为一个通过新鲜度校验的页面；新鲜空页可正常结束分页，缺失、陈旧、错误游标与超时仍保持失败。完整历史回填和持续增量语义不在 V0 范围内。

## 可复现检查

```text
cd apps/control-plane && npm ci
cd apps/control-plane && npm run lint && npm test && npm run build
PYTHONPATH=workers/v0/src python3.11 -m unittest discover -s workers/v0/tests -p 'test_*.py' -v
PYTHONPATH=spikes/spike_01:spikes/spike_02 python3.11 -m unittest discover -s spikes -p 'test_*.py' -v
V0_PYTHON_BIN=python3.11 bash scripts/v0/run-e2e.sh --mode deterministic --provider mock --chunk-size 100 --max-concurrency 5 --timeout-seconds 240 --max-attempts 3
supabase test db
bash scripts/v0/redact-check.sh
```

实际本机使用的 Python 3.12 运行时与上述 `python3.11` 命令等价；系统 Python 3.9 不满足 Worker 要求。

## V1 前置事项

- V0 的真实单页闭环已完成。若要进入 V1，必须先独立编写并批准 V1 Spec 与 Plan；本记录不授权 V1 实现。
- V1 需单独定义历史回填、持续增量、跨日期稳定性、登录失效/权限失败长期观察和容量/SLA 门槛；不得把本次单页验收外推为上述结论。
