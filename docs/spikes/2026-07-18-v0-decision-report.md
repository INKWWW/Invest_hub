# V0 最终报告——基础设施与技术验证

最后更新：2026-07-19

## 决策

**通过（有界单页真实 Discord 验证已完成）**。

V0 的最小闭环已在公开测试样例、Mock 提供方、本地 Supabase 和脱敏控制面边界中验证：邀请码与角色隔离、工作节点生命周期、单任务租约、原始数据 → 规范数据 → 结构化输出的证据链、检查点安全推进、提供方超时回收和管理员调试面均有可复现测试。2026-07-19 又完成隔离 Supabase 迁移、Vercel 预览部署、工作节点远程持久化收据和管理端操作入口；并在受保护预览上用明确标记为合成、非 Discord 的任务完成注册 → 心跳 → 领取 → 持久化 → 回报结果，数据库回读确认收据与检查点一致。远程补测还确认普通用户无法访问管理员接口，以及过期租约会由第二工作节点以第 2 次尝试重新领取并恢复预置检查点。

同日，已在用户明确授权的专用浏览器配置档和目标 Discord 页面完成真实验证。首次尝试在无界历史翻页时触发采集超时，被正确标记为可重试失败，未推进安全检查点；随后将 V0 验证批次限定为一个通过新鲜度校验的页面。第 2 次尝试成功完成真实采集 → 规范映射 → 本机 Codex CLI 结构化 → 远程持久化 → 结果回报：控制面显示 20 条原始记录、20 条规范记录、1 条 `codex_cli` 结构化运行、20 个消息证据关联、20 个本地原始引用关联，以及非空安全检查点。真实正文、频道地址、浏览器配置档、提示词、模型完整响应和本机证据均未写入仓库。

## 验收矩阵

| 验收项 | 结论 | 证据 / 命令 | 限制 |
| --- | --- | --- | --- |
| 跨语言契约拒绝非法字段 | 通过 | `tests://v0/contracts`；Worker 契约测试 | 只验证公开测试样例 |
| Supabase 数据结构、RLS、邀请码单次使用 | 通过 | `db://v0/rls/001-002`；`supabase test db`——55 条断言 | 远程仅确认迁移版本，不含真实数据 |
| 管理员/普通用户 API 与任务状态 | 通过 | `tests://v0/control-plane`；30 个 Vitest 测试；`deploy://v0/remote-role-recovery-20260719` | 远程补测使用临时普通用户，已删除 |
| Worker 注册/心跳/领取/持久化/结果 | 通过 | `tests://v0/worker`；46 个单元测试 | 仅覆盖确定性传输与模拟时钟边界 |
| Active Adapter 新鲜度/截止时间 | 通过 | `tests://v0/active-adapter` | 本报告未包含真实浏览器页面 |
| Mock/Codex 提供方边界 | 通过 | `tests://v0/provider`；进程组与数据结构测试 | 未新增真实 Codex 容量结论 |
| 媒体证据关联 | 通过 | `e2e://v0/deterministic`；精确来源 ID 断言 | 仅使用公开测试样例 |
| 管理员调试脱敏与重试门禁 | 通过 | `tests://v0/admin`；`npm run lint`；`npm run build` | 这是运维调试界面，不是普通用户阅读界面 |
| 确定性恢复 | 通过 | `e2e://v0/deterministic`；7 个测试 | 使用内存控制面，未验证已部署 HTTP |
| 仓库脱敏 | 通过 | `checks://v0/redaction`；`bash scripts/v0/redact-check.sh` | 检查凭据形态值，不替代语义审查 |
| 已授权真实 Discord 有界单页 | 通过 | `deploy://v0/real-discord-bounded-batch-20260719`；第 1 次超时后第 2 次成功 | 只验证一个新鲜页面；不代表历史回填或持续运营能力 |
| 隔离 Supabase 迁移与 Vercel Preview 构建 | 通过 | `deploy://v0/preview-ready`；远程迁移 `001/002`、Preview Ready | 不含生产数据或真实内容 |
| 已部署 V0 核心工作节点 HTTP | 通过 | `deploy://v0/http-smoke-20260719`；合成注册 → 心跳 → 领取 → 持久化 → 回报结果、回执与检查点回读 | 不替代真实 Discord |
| 已部署 V0 角色/恢复 | 通过 | `deploy://v0/remote-role-recovery-20260719`；普通用户管理员阻断、过期租约的第 2 次重新领取及检查点继承 | 全部验证对象已删除 |

## 已交付内容

- `contracts/v0` 下的契约数据结构与加载器。
- 基于 Next.js/Supabase 的控制面，包含按角色约束的管理员 API、任务/租约 RPC 集成和脱敏管理员页面。
- Python 3.11+ Worker，包含仅所有者可访问的配置/凭据存储、恢复状态、Active Adapter、Canonical 映射、检查点守卫和本地证据。
- Mock/Codex CLI 提供方边界，包含只读/临时调用、有界进程组超时清理、重试策略，以及严格的结构化输出/媒体来源校验。
- 确定性端到端测试框架、真实页面预检门禁、脱敏检查和 V0 环境模板。
- 隔离远程持久化协议：Worker 先提交原始保留引用、规范消息、结构化输出和证据关联；数据库核验持久化收据后才允许结果推进 checkpoint。
- 最小管理员操作入口：创建逻辑来源和手动同步任务，不收集 Discord URL、Profile、Prompt 或正文。

## 已知限制与 V1 门禁

V0 不交付普通用户正式阅读页、不接入 X、不提供多来源运营、不实现 GLM 或自动回退。本次真实验证只覆盖一个通过新鲜度校验的页面，不承担历史回填、跨日期稳定性或持续增量运营结论；这些能力必须在 V1 规格中重新定义并单独验收。远程部署仍必须使用隔离的 V0 项目和不含真实生产数据的凭据。V1 只能从新的规格与计划审批开始，不因本报告自动获得实现授权。

## 敏感数据声明

本报告不包含 Discord/X 正文、真实 URL、浏览器配置档路径、Cookie、Token、邀请码、提示词正文、完整模型响应或本地证据内容；报告中的证据引用均为逻辑标识。
